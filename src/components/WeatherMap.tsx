import React from 'react'
import { ForcastContainer, Text } from '../styled-components'

const WeatherMap = () => {
  return (
    <ForcastContainer flex={2}>
      <Text>Weather map</Text>
    </ForcastContainer>
  )
}

export default WeatherMap
